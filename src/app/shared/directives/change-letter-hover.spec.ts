import { ChangeLetterHover } from './change-letter-hover';

describe('ChangeLetterHover', () => {
  it('should create an instance', () => {
    const directive = new ChangeLetterHover();
    expect(directive).toBeTruthy();
  });
});
